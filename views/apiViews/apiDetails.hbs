<h2> {{pokes.name}} Details </h2>




{{#if isSaur}}
  <h4>Is True</h4>
  <img class="pokeImg" src="{{pokes.front_shiny}}" alt="{{pokes.name}} pic front shiny view">
  <img class="pokeImg" src="{{pokes.back_shiny}}" alt="{{pokes.name}} pic back shiny view">
{{else}}
  <h4>Is False</h4>
  <img class="pokeImg" src="{{pokes.front_default}}" alt="{{pokes.name}} pic front view">
  <img class="pokeImg" src="{{pokes.back_default}}" alt="{{pokes.name}} pic back view">
{{/if}}
